#ifndef UINT64_H
#define UINT64_H 1

#if @HAVE_STDINT_H@
# include <stdint.h>
typedef uint64_t uint64;
#elif @SIZEOF_LONG_LONG@ == 8
typedef unsigned long long uint64;
#elif @SIZEOF_LONG@ == 8
typedef unsigned long uint64;
#else
# error No suitable 64-bit integral type
#endif

#endif /* UINT64_H */
